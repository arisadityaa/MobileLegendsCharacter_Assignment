<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="id.arisaditya.mobilelegendscharacter">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_mobilelegends_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_mobilelegends_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.MobileLegendsCharacter">
        <activity
            android:name=".SplashScreen"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".About"
            android:exported="false" />
        <activity
            android:name=".DetailCharacter"
            android:exported="false" />
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
            </intent-filter>
        </activity>
    </application>

</manifest>